{
  "tools": [
    {
      "name": "create_file",
      "description": "Creates a new file with optional content",
      "parameters": {
        "filename": "string",
        "content": "string"
      }
    },
    {
      "name": "delete_file",
      "description": "Deletes a file if it exists",
      "parameters": {
        "filename": "string"
      }
    }
  ]
}
